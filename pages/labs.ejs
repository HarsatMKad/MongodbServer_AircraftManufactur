<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
</head>
<body style="background-color: #AFEEEE ">
  <form action="/" method="get">
    <input type="submit" value="Back">
  </form>

  <% for (let i = 0; i < data.length; i++) { %>
  <hr>
  <h2> Lab <%= i + 1 %></h2>
  <h4> work at workshop <%= data[i].workshop[0].number %> </h4>
  <h3> Testers: </h3>
  <% for(let j = 0; j < data[i].emps.length; j++) { %>
  <h4> <%= data[i].emps[j].lastname %> </h4>
  <% } %>
  <hr>
  <% } %>

</body>
</html>